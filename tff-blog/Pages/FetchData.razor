@page "/fetchdata"
@inject IHttpClientFactory ClientFactory

<h1>Weather forecast</h1>

<p>This component demonstrates fetching data from the server.</p>

<Markdown MarkdownContent="@markdowncontent" />

@code {
    private string markdowncontent;

    protected override async Task OnInitializedAsync()
    {
        var client = ClientFactory.CreateClient("Client");

        var markdownRequest = await client.GetAsync($"sample-data/markdowntext.md");

        markdowncontent = markdownRequest.IsSuccessStatusCode ? await markdownRequest.Content.ReadAsStringAsync() : null;
    }
}
