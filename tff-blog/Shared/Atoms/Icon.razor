<img class=@IconCssClass src="@IconImage" />

@code {
    [Parameter]
    public string IconImage { get; set; }

    [Parameter]
    public bool InvertColor { get; set; }

    private string IconCssClass => InvertColor ? "icon inverted" : "icon";
}
